//Q1: Write a program to input two numbers and display their sum.

#include<stdio.h>

int main()
{
    int x, y;
    printf("enter both the numbers: ");
    scanf("%d %d", &x , &y);
    printf("sum = %d \n", x + y);
    return 0;
}